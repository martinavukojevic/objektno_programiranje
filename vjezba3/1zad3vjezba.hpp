#ifndef vektor_hpp
#define vektor_hpp

#include<vector>;

void unosVektora(std::vector<int>&vektor, int BrojElemenata);
void unosVektora(std::vector<int>&vektor, int donjaGranica, int gornjaGranica);
void unosVektora(std::vector<int>&vektor);
#endif
